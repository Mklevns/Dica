# DICA core pipeline (CLI: python main.py ...)
# Python 3.11+ recommended (tomllib, modern typing).

pydantic>=2.7
httpx>=0.27
libcst>=1.4
tiktoken>=0.7
ruff>=0.4
mypy>=1.10

# Optional backends — install when you need them:
#   Gradio UI:     pip install -r requirements-ui.txt
#   Docker sandbox: pip install -r requirements-sandbox.txt
